# Legacy standalone pages

These files were moved out of the main demo root because the current portfolio version uses `dashboard.html` as the unified Seller Hub for products, orders, and reviews.

They are kept for reference only and should not be used as the public demo entry points.
